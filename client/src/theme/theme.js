import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    mode: 'dark',  // Enables dark mode
    primary: { main: '#2196f3' }, // Customize the primary color
    background: { default: '#303030' }, // Set a darker background color
  },
});
